import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
    paper: {
        padding: '1rem 2rem',
        margin: '0.5rem',
    },
    header: {
        marginBottom: '1.5rem',
    },
});
